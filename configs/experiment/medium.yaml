# @package _global_

model:
  params:
    wrapper_cfg:
      params:
        model_cfg:
          params:
            model_channels: 320 #number of intermediate layer channels across the U-Net
            attention_resolutions: [4, 2]